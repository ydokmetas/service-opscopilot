services:
  migrate:
    build: .
    command: ["alembic", "upgrade", "head"]
    environment:
      DATABASE_URL: postgresql+psycopg://opscopilot:localdev@db:5432/opscopilot
    depends_on:
      db:
        condition: service_healthy
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+psycopg://opscopilot:localdev@db:5432/opscopilot
      LOG_LEVEL: INFO
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  db:
    image: postgres:17
    environment:
      POSTGRES_USER: opscopilot
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: opscopilot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U opscopilot -d opscopilot"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: